[project]
name = "behav"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai",
    "numpy",
    "requests",
    "opencv-python",
    "matplotlib",
    "Pillow"
]
